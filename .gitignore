# Secrets
.env
*.env.local

# Data (user content — not part of the codebase)
data/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# IDE
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db
data

# AI tool config / scratch files (do not commit)
CLAUDE.md
AGENTS.md
AGENT.md
.claude/
.cursor/
.cursorrules
.aider*
.github-copilot*

# Local one-shot scripts — migrations, backfills, repair tools.
# These stay on disk but never get committed.
scripts/
# ...except the dev-tooling scripts that Makefile + pre-commit reference.
!scripts/ratchet_coverage.py
!scripts/render_bench_report.py

# Coverage + benchmark artifacts (regenerated by `make coverage` / `make bench`)
.coverage
coverage.xml
htmlcov/
.benchmarks/
docs/
